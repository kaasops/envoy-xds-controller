# permissions for end users to view tracings.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: envoy-xds-controller
    app.kubernetes.io/managed-by: kustomize
  name: tracing-viewer-role
rules:
- apiGroups:
  - envoy.kaasops.io
  resources:
  - tracings
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - envoy.kaasops.io
  resources:
  - tracings/status
  verbs:
  - get
